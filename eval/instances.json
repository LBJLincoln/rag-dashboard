[
  {
    "name": "hf-space-slow",
    "host": "https://lbjlincoln-nomos-rag-engine.hf.space",
    "pipelines": ["standard", "graph"],
    "batch_size": 5,
    "max_concurrent": 10,
    "early_stop": 15,
    "comment": "HF Space 16GB — Standard + Graph (slow RAG, ~40s/q, batch 5 optimal)"
  },
  {
    "name": "hf-space-fast",
    "host": "https://lbjlincoln-nomos-rag-engine.hf.space",
    "pipelines": ["quantitative", "orchestrator"],
    "batch_size": 10,
    "max_concurrent": 20,
    "early_stop": 15,
    "comment": "HF Space 16GB — Quant + Orch (fast, ~3-5s/q, batch 10 optimal)"
  },
  {
    "name": "codespace-rag-tests",
    "host": "http://localhost:5678",
    "pipelines": ["standard", "graph", "quantitative", "orchestrator"],
    "batch_size": 5,
    "max_concurrent": 20,
    "early_stop": 15,
    "ssh_tunnel": "gh codespace ssh --codespace nomos-rag-tests-5g6g5q9vjjwjf5g4 -- -L 15678:localhost:5678",
    "comment": "Codespace rag-tests 8GB — own n8n, own OpenRouter key, doubles throughput"
  },
  {
    "name": "codespace-data-ingestion",
    "host": "http://localhost:5678",
    "pipelines": ["standard", "graph"],
    "batch_size": 5,
    "max_concurrent": 10,
    "early_stop": 15,
    "ssh_tunnel": "gh codespace ssh --codespace <data-ingestion-cs> -- -L 25678:localhost:5678",
    "comment": "Codespace data-ingestion 8GB — own n8n, own OpenRouter key, triples throughput"
  },
  {
    "name": "pme-hf-space",
    "host": "https://lbjlincoln-nomos-rag-engine.hf.space",
    "pipelines": ["pme-gateway", "pme-action", "pme-whatsapp"],
    "batch_size": 3,
    "max_concurrent": 9,
    "early_stop": 10,
    "comment": "HF Space — PME workflows (lighter, need Google/Telegram creds)"
  }
]
